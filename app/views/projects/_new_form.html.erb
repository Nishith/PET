<%= form_for(@new_project) do |f| %>
  <% if @new_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_project.errors.count, "error") %> prohibited this new_project from being saved:</h2>

      <ul>
      <% @new_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :lifecycle_id %><br />
    <%= f.text_field :lifecycle_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
